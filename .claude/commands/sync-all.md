Run the sync-all script to distribute Claude settings across all workspace repos.

Execute the following command and report the output:

```bash
bash c:/GitHub/optivem/academy/github-utils/scripts/sync-all.sh
```

Report which repos were updated and whether settings were already in sync.
